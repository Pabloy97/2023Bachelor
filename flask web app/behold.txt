<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <canvas id="myChart" max-width="100" max-height="100">
        <script>
            const ctx = document.getElementById('myChart');

            new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
                    datasets: [{
                        label: 'Vulnerabilities',
                        data: [12, 19, 3, 5, 2, 3],
                        borderWidth: 1
                    }]
                },
                options: {
                    scales: {
                        y: {
                            beginAtZero: true
                        }
                    }
                }
            });
        </script>
    </canvas>
    <h1>Daily stats!</h1>
    <p>Here we can insert and compare data from previous hours/days/weeks/months/years</p>